import { data as houseData } from './house'
import { data as apartmentData } from './apartment'
import { ExposeConfiguration } from '@components/expose/types'

export const configurations: ExposeConfiguration[] = [houseData, apartmentData]
