const colors = require('./colors');
const textColor = require('./textColor');

module.exports = {
    extend: {
        colors,
        textColor,
    },
    boxShadow: {
        card: 'rgba(99, 99, 99, 0.2) 0px 2px 8px 0px',
    },
};
